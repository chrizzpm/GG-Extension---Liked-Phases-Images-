# DESIGN.md

## 气质与意象
- 产品：网页收藏家（Web Collector）Chrome 扩展 —— 把网页上的文字、图片"剪"下来，分类夹进收藏册。
- 意象锚点：**晨光下的手账剪报册**。米白纸张、墨绿钢笔圈点、朱砂色便利贴贴边、胶带纸感；像把杂志段落剪进本子、把旅行门票夹进书页。温暖、有"珍藏"的手感，而非冷冰冰的效率工具。
- 性格关键词：纸质、克制、利落、可信赖。

## 配色方案
- 纸色底 `--wc-paper: #FAF6EF`：米纸，浮层/页面底色
- 墨绿主色 `--wc-green: #1F6F54`，深墨绿 `#154E3B`：收藏按钮、主操作、品牌色
- 朱砂点缀 `--wc-vermilion: #E85D3F`：收藏角标脉冲、成功强调、新建目录按钮
- 墨色文字 `--wc-ink: #2B2A26`；次要灰褐 `#8A8378`
- 卡片白 `#FFFFFF`，配暖调柔和阴影（rgba(43,42,38,0.08~0.16)）
- 目录标签浅墨绿底 `#E4EFE9`

## 字体排版
- 标题：衬线栈 `Georgia, "Songti SC", "Noto Serif SC", "SimSun", serif` —— 剪报标题感
- 正文/UI：系统无衬线栈 `-apple-system, BlinkMacSystemFont, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif`
- 扩展内不加载远程字体（离线可用、符合 MV3 CSP）
- 节奏：标题 16~20px 粗衬线；正文 13~14px；行高 1.6

## 动效与交互
- 悬浮按钮/角标：轻微弹性弹出 `cubic-bezier(.2,.9,.3,1.3)`，120~180ms
- 模态：遮罩淡入 + 卡片上浮 8px；toast 从底部滑入
- 拖拽投放时悬浮球放大、墨绿→朱砂脉冲呼吸
- 统一"纸张翻动般干脆"：短、稳、无弹跳过度

## 组件规范
- content script 全部 UI 注入 **Shadow DOM**，样式与宿主页面完全隔离
- 圆角：卡片 12px、按钮 8px、角标圆形
- 图标一律内联 SVG（书签、加号、文件夹、搜索、垃圾桶），禁用 emoji 作功能图标

## 设计禁忌
- 禁止蓝紫渐变、霓虹发光、深色科技风
- 禁止纯白冷灰底（#FFFFFF 仅用于卡片表面）
- 禁止圆角超过 16px 的"软糖"风
- 禁止远程字体/CDN 依赖进入扩展运行时
